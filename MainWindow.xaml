<Window x:Name="Window" x:Class="SimpleLoadOrderOrganizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dd="urn:gong-wpf-dragdrop"
        xmlns:WindowChrome="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:res="clr-namespace:SimpleLoadOrderOrganizer.Resources"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleLoadOrderOrganizer"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MinHeight="600" MinWidth="400"
        Title="RAZOR" Height="700" Width="789"  Loaded="Window_Loaded">


    <Window.Resources>


        <local:GameIndexToImageConverter x:Key="GameIndexToImageConverter"/>


        <Style x:Key="BackgroundStyle" TargetType="Grid">
            <Setter Property="Background" Value="Black"/>
        </Style>
        <Style x:Key="SectionStyle" TargetType="Grid">
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="{StaticResource base}"/>

        </Style>
        <Style x:Key="TextblockStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="{StaticResource base}"/>

        </Style>

        <Style x:Key="ScrollBarButtonStyle" TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="10"/>
            <Setter Property="Margin" Value="0,8,0,0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand" 
                                          Style="{StaticResource ScrollBarButtonStyle}" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="Black" Opacity=".6" CornerRadius="5"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand" 
                                          Style="{StaticResource ScrollBarButtonStyle}" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- Adjust size for horizontal scrollbars -->
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                                <Setter Property="Height" Value="5"/>
                                <Setter Property="Width" Value="Auto"/>
                            </Trigger>
                            <!-- Vertical scrollbars remain reversed -->
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="True"/>
                                <Setter Property="Width" Value="10"/>
                                <Setter Property="Height" Value="Auto"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>







        <Style x:Key="ProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Foreground" Value="#222222"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#22FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border 
                    x:Name="OuterBorder" 
                    Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" 
                    CornerRadius="5"
                    SnapsToDevicePixels="True">
                            <Grid ClipToBounds="True">
                                <Border 
                            x:Name="PART_Track" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="5"/>
                                <Border 
                            x:Name="PART_Indicator" 
                            Background="{TemplateBinding Foreground}" 
                            HorizontalAlignment="Left" 
                            Width="0" 
                            CornerRadius="5"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>


        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <!-- left, top, right, bottom -->
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="{StaticResource base}"/>

            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="5"
                                Padding="0,0,0,3">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#444"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#66CCFF"/>
                            </Trigger>
                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#222"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00AACC"/>
                            </Trigger>
                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="AnimatedButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="{StaticResource base}"/>

            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="5"
                        Padding="0,0,0,1">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <!-- Hover animation -->
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="border"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#444444"
                                                Duration="0:0:0.25" />
                                        <ColorAnimation Storyboard.TargetName="border"
                                                Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                To="#66CCFF"
                                                Duration="0:0:0.25" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="border"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="Black"
                                                Duration="0:0:0.25" />
                                        <ColorAnimation Storyboard.TargetName="border"
                                                Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                To="Black"
                                                Duration="0:0:0.25" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#222"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00AACC"/>
                            </Trigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="white"/>
            <Setter Property="Opacity" Value="1"/>

            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border 
                    CornerRadius="5"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BannerStyle" TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="30"/>

            <Setter Property="FontFamily" Value="{StaticResource morrowind}"/>

        </Style>
        <Style x:Key="CheckboxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="{StaticResource base}"/>

            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Custom checkbox box -->
                            <Border x:Name="Border"
                            Width="18" Height="18"
                            CornerRadius="4"
                                    
                            Background="#111111"
                            BorderBrush="White"
                                    Opacity=".70"
                                     Padding="1,2,0,0"
                            BorderThickness="0"
                            Margin="0,0,0,0">
                                <!-- The check mark -->
                                <Path x:Name="CheckMark"
                              Data="M2,6 L7,11 L14,3"
                              Stroke="White"
                              StrokeThickness="2"
                              StrokeEndLineCap="Round"
                              StrokeStartLineCap="Round"
                              Visibility="Collapsed"/>
                            </Border>

                            <!-- The label text -->
                            <ContentPresenter VerticalAlignment="Center"
                                      RecognizesAccessKey="True"/>
                        </StackPanel>

                        <ControlTemplate.Triggers>
                            <!-- Checked -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Border" Property="Background" Value="Black"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                            </Trigger>

                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#66CCFF"/>
                            </Trigger>

                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#222"/>
                            </Trigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.5"/>
                                <Setter Property="Foreground" Value="#777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowStyle" TargetType="Window">

        </Style>
        <Style x:Key="ListviewStyle" TargetType="ListView">
            <Setter Property="Foreground" Value="white"/>

            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="SelectionMode" Value="Single"/>


        </Style>

        <!-- Replace your existing GridHeader style with this -->
        <Style x:Key="GridHeader" TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="{StaticResource base}"/>

            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#66FFFFFF"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                        <Grid>
                            <!-- main visual with bottom line -->
                            <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="Center"
                            Margin="0,15,0,15"
                            RecognizesAccessKey="True"/>
                            </Border>

                            <!-- Invisible but interactive resize gripper -->
                            <!-- Name MUST be PART_HeaderGripper for WPF to wire resizing -->
                            <Thumb x:Name="PART_HeaderGripper"
                           Width="10"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Stretch"
                           Cursor="SizeWE"
                           Background="Transparent"
                           IsHitTestVisible="True">

                                <!-- optional visual for gripper: appears only when mouse is over header -->
                                <Thumb.Template>
                                    <ControlTemplate TargetType="Thumb">
                                        <Grid>
                                            <!-- invisible default rect so Thumb remains hit-testable -->
                                            <Rectangle Fill="Transparent"/>

                                            <!-- subtle visible handle shown on header hover -->
                                            <Rectangle x:Name="Handle"
                                               Width="0"
                                               HorizontalAlignment="Right"
                                               Margin="0,0,0,0"
                                               RadiusX="1" RadiusY="1"
                                               Fill="#99FFFFFF"
                                               Opacity="0"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <!-- show handle when header is hovered -->
                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=GridViewColumnHeader}}" Value="True">
                                                <Setter TargetName="Handle" Property="Opacity" Value="1"/>
                                            </DataTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Thumb.Template>

                            </Thumb>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="Transparent"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#22FFFFFF"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6"
                        SnapsToDevicePixels="True">
                            <!-- 
                        IMPORTANT: GridViewRowPresenter ensures column widths line up.
                        Use HorizontalAlignment="Stretch" and no extra Margin/Padding.
                    -->
                            <GridViewRowPresenter
                        x:Name="RowPresenter"
                        Content="{TemplateBinding Content}"
                        Columns="{Binding Path=View.Columns, RelativeSource={RelativeSource AncestorType=ListView}}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        SnapsToDevicePixels="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <!-- Hover animation -->
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="Bd"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="#22FFFFFF"
                                                Duration="0:0:0.18"
                                                FillBehavior="HoldEnd"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="Bd"
                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                To="Transparent"
                                                Duration="0:0:0.18"
                                                FillBehavior="HoldEnd"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>

                            <!-- Selected animation -->
                            <Trigger Property="IsSelected" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="Bd"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="#44FFFFFF"
                                                    Duration="0:0:0.18"
                                                    FillBehavior="HoldEnd"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="Bd"
                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                    To="Transparent"
                                                    Duration="0:0:0.18"
                                                    FillBehavior="HoldEnd"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#88CCFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>




        <Style x:Key="ComboBoxTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid>
                            <Border CornerRadius="5,0,0,5"
                            BorderThickness="1,1,0,1"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Black">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ComboBoxButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border 
                        Background="White" 
                        x:Name="border" 
                        CornerRadius="0,5,5,0" 
                        BorderThickness="0,1,1,1"
                        BorderBrush="Black">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style  x:Key="ComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition MaxWidth="18"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Name="PART_EditableTextBox"
                             Style="{StaticResource ComboBoxTextBoxStyle}"
                             Padding="5,0,0,0"
                             Height="{TemplateBinding Height}"/>
                            <ToggleButton Grid.Column="1" Margin="0"
                            Height="{TemplateBinding Height}"
                            Style="{StaticResource ComboBoxButtonStyle}"
                            Focusable="False"
                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            ClickMode="Press">
                                <Path Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="Black" />
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite"
                            Content="{TemplateBinding SelectionBoxItem}"
                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            Margin="5,0,0,0"/>
                            <Popup Name="Popup"
                            Placement="Bottom"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            AllowsTransparency="True" 
                            Focusable="False"
                            PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                SnapsToDevicePixels="True"                
                                MinWidth="{TemplateBinding ActualWidth}"
                                MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border 
                                    x:Name="DropDownBorder"
                                    BorderThickness="1"
                                    CornerRadius="5"
                                    Background="Azure"
                                    BorderBrush="Black"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome
        ResizeBorderThickness="6"
        CaptionHeight="0"
        CornerRadius="10"
        GlassFrameThickness="0"
        UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>


    <Border CornerRadius="20"
            Background="Black"
         
            BorderThickness="2"
            Padding="10" Margin="0,63,0,0">

        <!-- STYLES -->



        <Grid Style="{StaticResource BackgroundStyle}" Margin="0,-2,0,0">



            <Grid.RowDefinitions>
                <RowDefinition Height="589*"/>
                <RowDefinition Height="26*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Background="black" Grid.Row="0" Margin="0,0,0,315">



                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>



                <TextBlock Text="RAZOR" 
                           FontFamily="{StaticResource morrowind}"
                           VerticalAlignment="Top" 
                           Margin="10,10,10,0"
                           Foreground="White"
                           FontWeight="Bold"/>



                <!-- Minimize -->
                <Button Grid.Column="1" Content="—" Width="40" Height="27"
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="Minimize_Click" VerticalAlignment="Top"/>

                <!-- Maximize -->
                <Button Grid.Column="2" Content="▢" Width="40"
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="Maximize_Click" Height="27" VerticalAlignment="Top"/>

                <!-- Close -->
                <Button Grid.Column="3" Content="✕" Width="40"
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        Click="Close_Click" Height="27" VerticalAlignment="Top"/>





            </Grid>



            <Border CornerRadius="10"
        Background="#222222" Margin="10,36,10,0" Height="214" VerticalAlignment="Top"
        >
                <Border CornerRadius="10"
     
    BorderThickness="2" Margin="-2,-2,-2,-2" Height="218"
    >

                    <Border.Background>
                        <!-- Faded background image -->
                        <ImageBrush 
ImageSource="{Binding ElementName=game, Path=SelectedIndex, 
                          Converter={StaticResource GameIndexToImageConverter}, 
                          ConverterParameter=top.png}"                        
                            Opacity="1"
                        Stretch="UniformToFill"/>
                    </Border.Background>

                    <Grid Style="{StaticResource SectionStyle}" Height="196" VerticalAlignment="Center" Margin="10,0,10,0">



                        <Label x:Name="gameFolderLabel" Style="{StaticResource LabelStyle}" Content="{x:Static res:Resources.folder}" HorizontalAlignment="Left" Margin="7,57,0,0" VerticalAlignment="Top" FontSize="14"/>
                        <Label x:Name="pluginLabel" Style="{StaticResource LabelStyle}" Content="{x:Static res:Resources.configFile}" HorizontalAlignment="Left" Margin="7,98,0,0" VerticalAlignment="Top" Height="61" FontSize="14"/>
                        <TextBox x:Name="gameFolderBox" Style="{StaticResource TextBoxStyle}" IsReadOnly="True" Text="{Binding GameFolder, Mode=TwoWay}" Margin="99,54,10,0"  TextWrapping="Wrap" VerticalAlignment="Top" Height="27" ToolTip="Displays game directory"  PreviewMouseDown="GameFolderBox_PreviewMouseDown"/>
                        <TextBox x:Name="pluginsTextBox" Style="{StaticResource TextBoxStyle}" IsReadOnly="True" Text="{Binding ConfigFolder, Mode=TwoWay}" Margin="99,98,10,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="27" ToolTip="Displays the directory for plugins.txt (or Morrowind.ini)" PreviewMouseDown="PluginsTextBox_PreviewMouseDown"/>
                        <ComboBox x:Name="game" Style="{StaticResource ComboBoxStyle}"  Margin="10,10,10,0" VerticalAlignment="Top" SelectedIndex="3" Height="27" >
                            <ComboBoxItem Content="The Elder Scrolls III: Morrowind"/>
                            <ComboBoxItem Content="The Elder Scrolls IV: Oblivion"/>
                            <ComboBoxItem Content="The Elder Scrolls V: Skyrim"/>
                            <ComboBoxItem Content="The Elder Scrolls V: Skyrim – Special Edition"/>
                            <ComboBoxItem Content="Fallout 3"/>
                            <ComboBoxItem Content="Fallout: New Vegas"/>
                            <ComboBoxItem Content="Fallout 4"/>
                        </ComboBox>
                        <CheckBox Style="{StaticResource CheckboxStyle}" x:Name="editMasters" IsChecked="{Binding EditMaster, Mode=TwoWay}" Content="{x:Static res:Resources.editMasters}" HorizontalAlignment="Left" Margin="12,143,0,0" VerticalAlignment="Top" >
                            <CheckBox.ToolTip>
                                <TextBlock>
              If checked, allows master plugins to be enabled/disabled
           <LineBreak/> 
              NOTE: SOME FILES WILL ALWAYS BE LOADED IN A SPECIFIC ORDER IF PRESENT, IF THESE FILES ARE 
           <LineBreak/>
              DISABLED THEY WILL AUTOMATICALLY BE ENABLED AND REORDERED THE NEXT TIME PLUGINS ARE LOADED
                                </TextBlock>
                            </CheckBox.ToolTip>
                        </CheckBox>
                        <CheckBox x:Name="conflictCheckBox" Style="{StaticResource CheckboxStyle}"  IsChecked="{Binding ConflictCheck, Mode=TwoWay}" Content="{x:Static res:Resources.checkForConflicts}" HorizontalAlignment="Left" Margin="12,173,0,0" VerticalAlignment="Top" ToolTip="After initially loading plugins, program will check each plugin and display what other plugins overlap with it" Checked="ConflictCheckBox_Checked"/>

                        <Button x:Name="saveButton" Style="{StaticResource AnimatedButtonStyle}" Content="{x:Static res:Resources.save}" HorizontalAlignment="Right" Margin="0,170,95,0" VerticalAlignment="Top" Click="SaveButton_Click" Width="58" Height="22"/>
                        <Button x:Name="playButton" Style="{StaticResource AnimatedButtonStyle}" Content="{x:Static res:Resources.play}" HorizontalAlignment="Right" Margin="0,170,10,4" Click="PlayButton_Click" Width="59"/>







                    </Grid>

                </Border>

            </Border>

            <Border CornerRadius="10"
                    Background="#222222" Margin="10,271,10,10"
                    >


                <Border CornerRadius="10"
     
                BorderThickness="2" Margin="-2,-2,-2,-2">

                    <Border.Background>
                        <!-- Faded background image -->
                        <ImageBrush  ImageSource="{Binding ElementName=game, Path=SelectedIndex, 
                          Converter={StaticResource GameIndexToImageConverter}, 
                          ConverterParameter=list.png}"                    
                        Opacity="1"
                    Stretch="UniformToFill"/>
                    </Border.Background>

                    <Grid>

                        <TextBlock x:Name="warningLabel" Style="{StaticResource TextblockStyle}" FontFamily="{StaticResource morrowind}"  TextWrapping="Wrap" RenderTransformOrigin="0.506,0.053" Visibility="Visible" Panel.ZIndex="-2" Margin="15,51,15,10" Text="{x:Static res:Resources.warning}" FontSize="14"></TextBlock>


                        <ListView x:Name="pluginsBox" ItemsSource="{Binding LoadOrder, Mode=TwoWay}" dd:DragDrop.IsDragSource="True" dd:DragDrop.IsDropTarget="True" Margin="15,-2,15,0" Drop="PluginsBox_Drop" >

                            <ListView.Resources>
                                <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource DarkScrollBar}"/>
                            </ListView.Resources>

                            <ListView.ItemContainerStyle>
                                <StaticResource ResourceKey="ListViewItemStyle"/>
                            </ListView.ItemContainerStyle>
                            <ListView.Style>
                                <StaticResource ResourceKey="ListviewStyle"/>
                            </ListView.Style>

                            <ListView.View>
                                <GridView  ColumnHeaderContainerStyle="{StaticResource GridHeader}">
                                    <GridViewColumn  Width="200" Header="{x:Static res:Resources.name}" >
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <CheckBox Content="{Binding PluginFilename}" IsChecked="{Binding IsActive}" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked">
                                                        <CheckBox.Style>

                                                            <Style TargetType="{x:Type CheckBox}">

                                                                <Setter Property="Foreground" Value="White"/>
                                                                <Setter Property="FontSize" Value="12"/>
                                                                <Setter Property="FontFamily" Value="{StaticResource base}"/>

                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="{x:Type CheckBox}">
                                                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                                                <!-- Custom checkbox box -->
                                                                                <Border x:Name="Border"
                                                                                Width="18" Height="18"
                                                                                CornerRadius="4"
                                                                                Background="#111111"
                                                                                BorderBrush="White"
                                                                                BorderThickness="0"
                                                                                    Opacity=".70"
                                                                                    Padding="1,2,0,0"
                                                                                Margin="0,0,6,0">
                                                                                    <!-- The check mark -->
                                                                                    <Path x:Name="CheckMark"
                                                                                  Data="M2,6 L7,11 L14,3"
                                                                                  Stroke="White"
                                                                                  StrokeThickness="2"
                                                                                    
                                                                                  StrokeEndLineCap="Round"
                                                                                  StrokeStartLineCap="Round"
                                                                                  Visibility="Collapsed"/>
                                                                                </Border>

                                                                                <!-- The label text -->
                                                                                <ContentPresenter VerticalAlignment="Center"
                          RecognizesAccessKey="True"/>
                                                                            </StackPanel>

                                                                            <ControlTemplate.Triggers>
                                                                                <!-- Checked -->
                                                                                <Trigger Property="IsChecked" Value="True">
                                                                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                                                                    <Setter TargetName="Border" Property="Background" Value="Black"/>
                                                                                    <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                                                                                </Trigger>

                                                                                <!-- Hover -->
                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                    <Setter TargetName="Border" Property="BorderBrush" Value="#66CCFF"/>
                                                                                </Trigger>

                                                                                <!-- Pressed -->
                                                                                <Trigger Property="IsPressed" Value="True">
                                                                                    <Setter TargetName="Border" Property="Background" Value="#222"/>
                                                                                </Trigger>

                                                                                <!-- Disabled -->
                                                                                <Trigger Property="IsEnabled" Value="False">
                                                                                    <Setter TargetName="Border" Property="Opacity" Value="0.5"/>
                                                                                    <Setter Property="Foreground" Value="#777"/>
                                                                                </Trigger>
                                                                            </ControlTemplate.Triggers>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Setter Property="Margin" Value="15,15"/>

                                                                <Setter Property="Foreground" Value="White"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsMaster}" Value="True">
                                                                        <Setter Property="IsEnabled" Value="{Binding IsChecked, ElementName=editMasters}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </CheckBox.Style>
                                                    </CheckBox>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn DisplayMemberBinding="{Binding IsMaster}" Header="{x:Static res:Resources.master}" Width="50"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding IsLight}" Header="{x:Static res:Resources.lightPlugin}" Width="75"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding OverrideRecords}" Header="{x:Static res:Resources.overrideRecords}" Width="100"/>
                                    <GridViewColumn  Header="{x:Static res:Resources.masters}" Width="100" >
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <ScrollViewer Height="50"  HorizontalScrollBarVisibility="Disabled"  VerticalScrollBarVisibility="Hidden">
                                                        <TextBlock Text="{Binding MastersString}" />
                                                    </ScrollViewer>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn  Header="{x:Static res:Resources.conflicts}" Width="105" >
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel>
                                                    <ScrollViewer Height="50"  HorizontalScrollBarVisibility="Disabled"  VerticalScrollBarVisibility="Hidden">
                                                        <TextBlock Text="{Binding Conflicts}" />
                                                    </ScrollViewer>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>

                                </GridView>
                            </ListView.View>

                        </ListView>

                    </Grid>
                </Border>

            </Border>


            <Label x:Name="progressLabel" Style="{StaticResource LabelStyle}" FontFamily="{StaticResource morrowind}" Content="©-2025-KARYUUDO-DIGITAL" HorizontalAlignment="Left" Margin="10,0,0,0" Width="343"  VerticalAlignment="Center" Grid.Row="1" Height="25" />
            <ProgressBar Style="{StaticResource ProgressBarStyle}" Minimum="0" Maximum="1.0" x:Name="loadingBar" Margin="182,0,10,0"  VerticalAlignment="Center" Height="30" RenderTransformOrigin="0.5,0.5" Visibility="Hidden" Grid.Row="1" >
                <ProgressBar.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="-1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </ProgressBar.RenderTransform>
            </ProgressBar>





        </Grid>

    </Border>

</Window>
   
